"""Browser journeys against the deployed stand.

A package (unlike the sibling `api/` directory) so the journey modules can use
relative imports for their page objects — `from .pages.login_page import ...`.
"""
