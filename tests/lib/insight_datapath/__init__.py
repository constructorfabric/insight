"""The data-path suite's pure fixture machinery: `$ref` resolution, schema
validation and spec loading. No database, no network — everything here is
computable from files, which is what lets its tests run as an ordinary unit lane.
"""
