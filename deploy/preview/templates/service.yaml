apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-preview.fullname" . }}
  labels:
    {{- include "insight-preview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-preview.selectorLabels" . | nindent 4 }}
