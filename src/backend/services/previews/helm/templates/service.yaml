apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-previews.fullname" . }}
  labels:
    {{- include "insight-previews.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-previews.selectorLabels" . | nindent 4 }}
