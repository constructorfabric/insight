sources:
  - source:
      key: ai_usage
      kind: managed_observation
      source_ref: ai_metric_observations
      evidence_ref: ai_metric_evidence
      # Longest lookback_window among the connectors feeding this source:
      # claude-enterprise re-reads 3 days, claude-team and chatgpt-team 2.
      revision_window_days: 3
    measures:
      - key: accepted_lines
        evidence_granularity: source_summary
      - key: removed_lines
        evidence_granularity: source_summary
      - key: active_day
        evidence_granularity: derived_population
      - key: cost_usd
        evidence_granularity: source_summary
      - key: accepted_edit_actions
        evidence_granularity: source_summary
      - key: tool_use_offered
        evidence_granularity: source_summary
      - key: assistant_messages
        evidence_granularity: source_summary
      - key: assistant_actions
        evidence_granularity: source_summary
      - key: dev_conversations
        evidence_granularity: source_summary
      - key: chat_assistant_conversations
        evidence_granularity: source_summary
      - key: prs_with_assistant
        evidence_granularity: source_summary
      - key: prs_total
        evidence_granularity: source_summary
    dimensions:
      - tool
      - surface
      - seat_status
  - source:
      key: git
      kind: managed_observation
      source_ref: git_metric_observations
      evidence_ref: git_metric_evidence
    measures:
      - key: commit_count
        evidence_granularity: event
        evidence_presentation: &commit_row
          show_value: false
          detail_columns:
            - { key: ref, label: Ref }
            - { key: title, label: Title }
            - { key: repository, label: Repository }
            - { key: author, label: Author }
            - { key: branch_scope, label: Branch scope }
            - { key: lines_added, label: Lines added, type: number }
            - { key: lines_removed, label: Lines removed, type: number }
      - key: commit_day
        evidence_granularity: derived_population
      - key: commit_change_size
        evidence_granularity: event
        evidence_presentation: *commit_row
      - key: code_lines_added
        evidence_granularity: source_summary
      - key: test_lines_added
        evidence_granularity: source_summary
      - key: test_and_code_lines_added
        evidence_granularity: source_summary
      - key: lines_added
        evidence_granularity: source_summary
      - key: lines_removed
        evidence_granularity: source_summary
      - key: pr_created
        evidence_granularity: event
        evidence_presentation: &pull_request_counted
          show_value: false
          detail_columns: &pull_request_columns
            - { key: ref, label: Ref }
            - { key: title, label: Title }
            - { key: repository, label: Repository }
            - { key: author, label: Author }
            - { key: branch_scope, label: Branch scope }
            - { key: destination_branch, label: Target branch }
      - key: pr_created_merged
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_abandoned
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_reviewed
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_reviewer_count
        evidence_granularity: event
        evidence_presentation: &pull_request_measured
          show_value: true
          detail_columns: *pull_request_columns
      - key: pr_multi_reviewed
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_merged
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_merged_without_approval
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: pr_commit_count
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_cycle_hours
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_change_size
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_first_review_hours
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_review_wait_share
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_review_to_merge_hours
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: pr_approval_to_merge_hours
        evidence_granularity: event
        evidence_presentation: *pull_request_measured
      - key: review_submitted
        evidence_granularity: event
        # A review is not classified by where the request landed, so these rows
        # carry the branch they targeted but no branch scope of their own.
        evidence_presentation: &review_row
          show_value: false
          detail_columns:
            - { key: ref, label: Ref }
            - { key: title, label: Title }
            - { key: repository, label: Repository }
            - { key: author, label: Author }
            - { key: destination_branch, label: Target branch }
      - key: pr_comment
        evidence_granularity: event
        evidence_presentation: *review_row
      - key: default_commit_count
        evidence_granularity: event
        evidence_presentation: *commit_row
      - key: non_default_commit_count
        evidence_granularity: event
        evidence_presentation: *commit_row
      - key: default_code_lines_added
        evidence_granularity: source_summary
      - key: non_default_code_lines_added
        evidence_granularity: source_summary
      - key: default_lines_added
        evidence_granularity: source_summary
      - key: non_default_lines_added
        evidence_granularity: source_summary
      - key: default_lines_removed
        evidence_granularity: source_summary
      - key: non_default_lines_removed
        evidence_granularity: source_summary
      - key: default_pr_created
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: non_default_pr_created
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: default_pr_merged
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
      - key: non_default_pr_merged
        evidence_granularity: event
        evidence_presentation: *pull_request_counted
    dimensions:
      - branch_scope
      - category
      - change_type
      - comment_target
      - destination_branch
      - file_extension
      - project
      - repository
      - source
  - source:
      key: collab
      kind: managed_observation
      source_ref: collab_metric_observations
      evidence_ref: collab_metric_evidence
    measures:
      - key: total_chat_messages
        evidence_granularity: source_summary
      - key: channel_posts
        evidence_granularity: source_summary
      - key: direct_and_group_messages
        evidence_granularity: source_summary
      - key: meeting_hours
        evidence_granularity: source_summary
      - key: meetings_attended
        evidence_granularity: source_summary
      - key: meetings_organized
        evidence_granularity: source_summary
      - key: adhoc_meetings_attended
        evidence_granularity: source_summary
      - key: scheduled_meetings_attended
        evidence_granularity: source_summary
      - key: emails_sent
        evidence_granularity: source_summary
      - key: emails_received
        evidence_granularity: source_summary
      - key: emails_read
        evidence_granularity: source_summary
      - key: files_engaged
        evidence_granularity: source_summary
      - key: files_shared_internal
        evidence_granularity: source_summary
      - key: files_shared_external
        evidence_granularity: source_summary
      - key: files_shared
        evidence_granularity: source_summary
      - key: active_day
        evidence_granularity: derived_population
      - key: active_modality
        evidence_granularity: derived_population
      - key: chat_active_day
        evidence_granularity: source_summary
      - key: meeting_free_day
        evidence_granularity: derived_population
      - key: focus_hours
        evidence_granularity: derived_population
      - key: working_hours
        evidence_granularity: derived_population
    dimensions:
      - tool
      - scope
  - source:
      key: task
      kind: managed_observation
      source_ref: task_metric_observations
      evidence_ref: task_metric_evidence
    measures:
      - key: tasks_closed
        evidence_granularity: event
        evidence_presentation: &issue_counted
          show_value: false
          detail_columns: &issue_columns
            - { key: ref, label: Ref }
            - { key: title, label: Title }
      - key: bugs_fixed
        evidence_granularity: event
        evidence_presentation: *issue_counted
      - key: closed_non_bug
        evidence_granularity: event
        evidence_presentation: *issue_counted
      - key: due_date_on_time
        evidence_granularity: event
        evidence_presentation: *issue_counted
      - key: due_date_with_due
        evidence_granularity: event
        evidence_presentation: *issue_counted
      - key: slip_days_total
        evidence_granularity: event
        evidence_presentation: &issue_measured
          show_value: true
          detail_columns: *issue_columns
      - key: late_count
        evidence_granularity: event
        evidence_presentation: *issue_counted
      - key: estimation_error_pct
        evidence_granularity: derived_population
      - key: estimation_samples
        evidence_granularity: derived_population
      - key: flow_dev_seconds
        evidence_granularity: derived_population
      - key: flow_lead_seconds
        evidence_granularity: derived_population
      - key: close_events
        evidence_granularity: derived_population
      - key: reopened_within_14d
        evidence_granularity: derived_population
      - key: worklog_seconds
        evidence_granularity: derived_population
      - key: in_progress_seconds
        evidence_granularity: derived_population
      - key: stale_in_progress
        evidence_granularity: derived_population
      - key: dev_time_hours
        evidence_granularity: event
        evidence_presentation: *issue_measured
      - key: resolution_days
        evidence_granularity: event
        evidence_presentation: *issue_measured
      - key: pickup_days
        evidence_granularity: event
        evidence_presentation: *issue_measured
    dimensions:
      - type
      - source
  - source:
      key: wiki
      kind: managed_observation
      source_ref: wiki_metric_observations
      evidence_ref: wiki_metric_evidence
    measures:
      - key: pages_created
        evidence_granularity: event
        evidence_presentation:
          show_value: false
          detail_columns:
            - { key: ref, label: Ref }
            - { key: title, label: Title }
      - key: edits
        evidence_granularity: source_summary
      - key: pages_edited
        evidence_granularity: source_summary
      - key: comments
        evidence_granularity: source_summary
    dimensions: []
  - source:
      key: ai_cost
      kind: managed_observation
      source_ref: ai_cost_metric_observations
      evidence_ref: ai_cost_metric_evidence
      # The month in progress is revisable throughout, and 31 days is the
      # longest such month.
      revision_window_days: 31
    measures:
      - key: extra_usage_usd
        evidence_granularity: source_summary
        evidence_presentation: &seat_month_row
          show_value: true
          detail_columns:
            - { key: billing_month, label: Billing month }
            - { key: ceiling_usd, label: Ceiling, type: number }
      - key: extra_usage_limit_usd
        evidence_granularity: source_summary
        evidence_presentation: *seat_month_row
      - key: seat_cost_usd
        evidence_granularity: source_summary
        evidence_presentation: *seat_month_row
      - key: daily_extra_usage_usd
        evidence_granularity: source_summary
        evidence_presentation:
          show_value: true
          detail_columns:
            - { key: billing_month, label: Billing month }
            - { key: month_to_date_usd, label: Month to date, type: number }
            - { key: covers_days, label: Days covered, type: number }
    dimensions:
      - tool
      - seat_tier
  - source:
      key: ci
      kind: managed_observation
      source_ref: ci_metric_observations
      evidence_ref: ci_metric_evidence
    measures:
      - key: runs
        evidence_granularity: event
        evidence_presentation: &ci_run_counted
          show_value: false
          detail_columns: &ci_run_columns
            - { key: repository, label: Repository }
            - { key: pipeline, label: Pipeline }
            - { key: branch, label: Branch }
            - { key: outcome, label: Outcome }
      - key: gate_runs
        evidence_granularity: event
        evidence_presentation: *ci_run_counted
      - key: gate_passed
        evidence_granularity: event
        evidence_presentation: *ci_run_counted
      - key: gate_first_try_passed
        evidence_granularity: event
        evidence_presentation: *ci_run_counted
      - key: gate_retried
        evidence_granularity: event
        evidence_presentation: *ci_run_counted
      - key: run_duration_min
        evidence_granularity: event
        evidence_presentation: &ci_run_measured
          show_value: true
          detail_columns: *ci_run_columns
      - key: run_hours
        evidence_granularity: event
        evidence_presentation: *ci_run_measured
      - key: deployments
        evidence_granularity: event
        evidence_presentation:
          show_value: false
          detail_columns:
            - { key: repository, label: Repository }
            - { key: environment, label: Environment }
            - { key: outcome, label: Outcome }
            - { key: env_kind, label: Environment type }
      - key: runs_matched_commit
        evidence_granularity: event
        evidence_presentation: *ci_run_counted
      - key: commits_observed
        evidence_granularity: event
        evidence_presentation:
          show_value: false
          detail_columns:
            - { key: repository, label: Repository }
    dimensions:
      - repository
      - pipeline
      - trigger
      - outcome
      - environment
      - env_kind
      - hour_block
metrics:
  - metric_key: ci.runs
    source_key: ci
    subject: pipelines
    tags:
      - ci
      - volume
    label: CI runs
    short_label: Runs
    description: Pipeline runs with a decided outcome
    explanation: Every CI pipeline run with a decided outcome, dated by the day the run started, whatever its trigger. Runs still in progress are not counted until they decide. History accumulates from when collection started; the source API retains only a bounded window, so earlier runs were never observable.
    format: integer
    direction: higher_is_better
    entity_type: tenant
    computation: sum
    inputs:
      - input_role: value
        measure_key: runs
    dimensions:
      - repository
      - pipeline
      - trigger
      - outcome
      - hour_block
  - metric_key: ci.gate_pass_rate
    source_key: ci
    subject: reliability
    tags:
      - ci
      - quality
    label: Gate pass rate
    short_label: Pass rate
    description: Share of commit-triggered runs that passed
    explanation: Success share of gate runs — runs triggered by a push, a pull request, or the merge queue that reached a decided outcome (success, failure, or timeout). Cancelled and skipped runs decide nothing and stay out of both sides; runs waiting for a manual approval never executed and stay out as well.
    format: percent
    direction: higher_is_better
    entity_type: tenant
    computation: !ratio
      scale: 100.0
    inputs:
      - input_role: numerator
        measure_key: gate_passed
      - input_role: denominator
        measure_key: gate_runs
    dimensions:
      - repository
      - pipeline
      - hour_block
  - metric_key: ci.gate_first_try_pass_rate
    source_key: ci
    subject: reliability
    tags:
      - ci
      - quality
    label: First-try pass rate
    short_label: First try
    description: Share of gate runs green without a retry
    explanation: Gate runs that passed on their first attempt, as a share of all gate runs. The gap to the plain pass rate is the retry tax — green that had to be bought with a re-run.
    format: percent
    direction: higher_is_better
    entity_type: tenant
    computation: !ratio
      scale: 100.0
    inputs:
      - input_role: numerator
        measure_key: gate_first_try_passed
      - input_role: denominator
        measure_key: gate_runs
    dimensions:
      - repository
      - pipeline
  - metric_key: ci.gate_retry_share
    source_key: ci
    subject: reliability
    tags:
      - ci
      - quality
    label: Retried gate runs
    short_label: Retried
    description: Share of gate runs that were re-run
    explanation: Gate runs whose final state came from a re-run, as a share of all gate runs. A retried run counts once, at its last attempt.
    format: percent
    direction: lower_is_better
    entity_type: tenant
    computation: !ratio
      scale: 100.0
    inputs:
      - input_role: numerator
        measure_key: gate_retried
      - input_role: denominator
        measure_key: gate_runs
    dimensions:
      - repository
      - pipeline
  - metric_key: ci.run_duration_min
    source_key: ci
    subject: duration
    tags:
      - ci
      - duration
      - distribution
    label: Gate run duration
    short_label: Duration
    description: Typical minutes a gate run takes
    explanation: Median wall-clock minutes from a gate run's start to its decision, over decided runs dated by the day they started. Wall clock, not billable compute. Instant failures that never really ran are excluded from durations while still counting toward rates.
    unit: min
    format: decimal
    direction: lower_is_better
    entity_type: tenant
    computation: median
    inputs:
      - input_role: value
        measure_key: run_duration_min
    dimensions:
      - repository
      - pipeline
      - outcome
  - metric_key: ci.run_duration_min_p90
    source_key: ci
    subject: duration
    tags:
      - ci
      - duration
      - distribution
    label: Gate run duration p90
    short_label: Duration p90
    description: Minutes the slowest tenth of gate runs exceed
    explanation: The 90th percentile of gate run duration — nine in ten runs finish faster. The gap to the median is the bad-day penalty a developer actually waits for.
    unit: min
    format: decimal
    direction: lower_is_better
    entity_type: tenant
    computation: !percentile
      q: 0.9
    inputs:
      - input_role: value
        measure_key: run_duration_min
    dimensions:
      - repository
      - pipeline
  - metric_key: ci.run_duration_min_stddev
    source_key: ci
    subject: duration
    tags:
      - ci
      - duration
      - distribution
    label: Gate run duration spread
    short_label: Duration spread
    description: How unevenly gate run durations vary
    explanation: Sample standard deviation of gate run durations, in minutes. A pipeline whose duration swings wildly is harder to plan around than a slow but steady one.
    unit: min
    format: decimal
    direction: lower_is_better
    entity_type: tenant
    computation: !stddev
    inputs:
      - input_role: value
        measure_key: run_duration_min
    dimensions:
      - repository
      - pipeline
  - metric_key: ci.run_hours
    source_key: ci
    subject: compute
    tags:
      - ci
      - volume
    label: CI hours
    short_label: CI hours
    description: Wall-clock hours spent running CI
    explanation: Wall-clock hours all decided runs spent executing, whatever the trigger. Not billable compute minutes — parallel jobs inside one run count as the run's elapsed time, not their sum. Filter by outcome to see hours burned on red runs.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: tenant
    computation: sum
    inputs:
      - input_role: value
        measure_key: run_hours
    dimensions:
      - repository
      - pipeline
      - outcome
  - metric_key: ci.runs_matched_commit
    source_key: ci
    subject: data_trust
    tags:
      - ci
      - coverage
    label: Runs matching a collected commit
    short_label: Runs matched
    description: Decided runs whose head commit was collected
    explanation: Decided runs whose head commit exists among the collected git commits. PR runs build synthetic merge refs and fork commits the commit stream never sees, so the gap between this and total runs is the honest ceiling for ANY analysis that joins CI to git history — chart it next to runs and collected commits to see how far the two streams can be trusted to join.
    format: integer
    direction: higher_is_better
    entity_type: tenant
    computation: sum
    inputs:
      - input_role: value
        measure_key: runs_matched_commit
    dimensions: []
  - metric_key: ci.commits_observed
    source_key: ci
    subject: data_trust
    tags:
      - ci
      - coverage
    label: Commits collected
    short_label: Commits
    description: Git commits collected, by commit date
    explanation: Commits the git connector collected, dated at their commit time — the other side of the run-to-commit join. Where runs far outnumber matched runs while commits stay high, CI is running on refs the commit stream does not see (PR merge refs, forks), not on missing history.
    format: integer
    direction: neutral
    entity_type: tenant
    computation: sum
    inputs:
      - input_role: value
        measure_key: commits_observed
    dimensions: []
  - metric_key: ci.deployments
    source_key: ci
    subject: deployments
    tags:
      - ci
      - delivery
    label: Deployments
    short_label: Deploys
    description: Deployments created, by outcome
    explanation: Deployments recorded in the period. The outcome is the latest status the deployment reached; one with no status yet shows as pending rather than being rounded away.
    format: integer
    direction: higher_is_better
    entity_type: tenant
    computation: sum
    inputs:
      - input_role: value
        measure_key: deployments
    dimensions:
      - repository
      - environment
      - outcome
      - env_kind
  - metric_key: ai.accepted_lines
    source_key: ai_usage
    subject: code
    label: AI-added lines
    short_label: AI lines +
    description: Accepted added coding output
    explanation: Accepted AI-generated added lines across coding AI tools.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: accepted_lines
    dimensions:
      - tool
  - metric_key: ai.removed_lines
    source_key: ai_usage
    subject: code
    label: AI-removed lines
    short_label: AI lines −
    description: Accepted deleted coding output
    explanation: Accepted AI-generated removed lines across coding AI tools.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: removed_lines
    dimensions:
      - tool
  - metric_key: ai.active_days
    source_key: ai_usage
    subject: activity
    label: AI active days
    short_label: AI days
    description: Days with any AI activity across dev and assistant tools
    explanation: Distinct days with person-attributed AI activity across dev and assistant tools.
    unit: days
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: active_day
    dimensions: []
  - metric_key: ai.cost
    source_key: ai_usage
    subject: cost
    label: AI potential usage cost
    short_label: Potential cost
    description: Potential cost of all AI usage at vendor rates, never the actual invoice
    explanation: Person-attributed AI usage priced at the vendor's token rates — what the consumption would cost if billed purely by usage. It includes usage a seat already covered and excludes seat fees, so it is not the invoiced amount, and only tools that price usage per person contribute. Never add it to actual usage cost, which is the billed part of this same consumption.
    format: currency
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: cost_usd
    dimensions:
      - tool
  - metric_key: ai.seat_cost
    source_key: ai_cost
    subject: cost
    label: AI seat cost
    short_label: Seat cost
    description: What a seat costs before any usage
    explanation: The invoiced price of one seat for a billing month, from the invoice's per-seat amount. Dated at that month's first day, so a window must hold it to return the fee. The month comes from the read, not from the vendor. A seat whose tier the invoice does not price returns no value.
    format: currency
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: seat_cost_usd
    dimensions:
      - tool
      - seat_tier
  - metric_key: ai.extra_usage_cost
    source_key: ai_cost
    subject: cost
    label: AI actual usage cost
    short_label: Actual cost
    description: Monthly cost billed on top of the seat fee, as last read
    explanation: What the vendor billed on top of the seat fee, once included usage ran out. Exact as of that month's last reading, dated at its first day, so a window missing that day returns nothing — read the per-day distribution. The month comes from the read, not the vendor. Never added to potential usage cost.
    format: currency
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: extra_usage_usd
    dimensions:
      - tool
      - seat_tier
  - metric_key: ai.daily_approximate_extra_usage_cost
    source_key: ai_cost
    subject: cost
    label: AI actual usage cost — approximate distribution
    short_label: Actual cost / day
    description: The monthly billed cost spread across its days
    explanation: The billed extra-usage cost placed on the days spent. Only a month-to-date total is reported, so a day is the step between readings — exact in sum against it, approximate in placement. The month comes from the read, so the 1st can cover several days. No reading means no point, and none is negative.
    format: currency
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: daily_extra_usage_usd
    dimensions:
      - tool
      - seat_tier
  - metric_key: ai.extra_usage_utilisation
    source_key: ai_cost
    subject: cost
    label: Extra-usage ceiling used
    short_label: Ceiling used
    description: Share of the extra-usage ceiling consumed
    explanation: Extra usage measured against the seat's ceiling. At 100% the vendor stops the seat, so this reads as proximity to being blocked, not waste. A seat with no ceiling returns no value, not zero. Above 100%, the ceiling was lowered below what the seat had spent. Its month comes from the read, not the vendor.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: extra_usage_usd
      - input_role: denominator
        measure_key: extra_usage_limit_usd
    dimensions:
      - tool
      - seat_tier
  - metric_key: ai.accepted_edit_actions
    source_key: ai_usage
    subject: code
    label: Accepted AI edits
    short_label: AI edits
    description: Accepted tool or edit suggestions
    explanation: Accepted AI edit or tool suggestions across supported coding AI tools.
    unit: actions
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: accepted_edit_actions
    dimensions:
      - tool
  - metric_key: ai.tool_acceptance_rate
    source_key: ai_usage
    subject: code
    tags:
      - rate
    label: AI tool acceptance
    short_label: AI accept %
    description: Accepted divided by offered AI edits
    explanation: Accepted AI edit or tool suggestions divided by offered suggestions.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: accepted_edit_actions
      - input_role: denominator
        measure_key: tool_use_offered
    dimensions:
      - tool
  - metric_key: ai.assistant_messages
    source_key: ai_usage
    subject: assistant
    label: AI assistant messages
    short_label: AI msgs
    description: Assistant messages
    explanation: Person-attributed assistant messages from supported AI assistant tools.
    unit: messages
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: assistant_messages
    dimensions:
      - tool
      - surface
  - metric_key: ai.assistant_actions
    source_key: ai_usage
    subject: assistant
    label: AI assistant actions
    short_label: AI actions
    description: Assistant actions
    explanation: Person-attributed assistant actions from supported AI assistant tools.
    unit: actions
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: assistant_actions
    dimensions:
      - tool
      - surface
  - metric_key: ai.dev_conversations
    source_key: ai_usage
    subject: assistant
    label: AI dev conversations
    short_label: AI dev chats
    description: Coding tool conversations where the source reports them
    explanation: Person-attributed conversations with coding AI tools, counted as the vendor counts them. For the agent tools this is the session or thread count — a session is the unit of conversation there, and no vendor publishes a separate conversation counter — so the number reads as "times the person started working with the assistant", not as messages exchanged. Tools that report no such unit, inline-completion tools among them, return no value rather than a zero.
    unit: conversations
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: dev_conversations
    dimensions:
      - tool
  - metric_key: ai.chat_assistant_conversations
    source_key: ai_usage
    subject: assistant
    label: AI chat conversations
    short_label: AI chats
    description: Chat assistant conversations
    explanation: Person-attributed chat assistant conversations from supported AI chat tools.
    unit: conversations
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: chat_assistant_conversations
    dimensions:
      - tool
      - surface
  - metric_key: ai.prs_with_assistant
    source_key: ai_usage
    subject: pull_requests
    label: PRs with AI assistance
    short_label: PRs with AI
    description: Pull requests the coding assistant touched
    explanation: Pull requests where the coding assistant was active at least once, as the vendor attributes them. Reported only by sources that connect to the code host themselves, so a person working without that connection returns no value rather than a zero. Counts pull requests, not commits or lines, and says nothing about how much of the change the assistant wrote.
    unit: PRs
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: prs_with_assistant
    dimensions:
      - tool
      - seat_status
  - metric_key: ai.prs_total
    source_key: ai_usage
    subject: pull_requests
    label: PRs seen by the AI vendor
    short_label: PRs seen
    description: Pull requests the AI vendor counted in the same window
    explanation: Pull requests the AI vendor observed for the person, served as the context for PRs with AI assistance rather than as a goal of its own. It is the vendor's count over the vendor's own window, which need not be the day it is reported against and need not agree with the git sources — read it next to that measure, not next to git pull-request metrics.
    unit: PRs
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: prs_total
    dimensions:
      - tool
      - seat_status
  - metric_key: git.commits
    source_key: git
    subject: commits
    label: Commits
    description: Authored commits
    explanation: Distinct authored commits across connected git sources, excluding merge commits.
    unit: commits
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: commit_count
    dimensions:
      - branch_scope
      - project
      - repository
      - source
  - metric_key: git.default_branch_commits
    source_key: git
    subject: commits
    label: Commits that reached the default branch
    short_label: Commits (default)
    description: Authored commits that reached the default branch
    explanation: >-
      Authored commits reachable from the repository's default branch, or
      carried onto it by a merged pull request. A commit joins this metric when
      its branch lands, so a past period's figure rises as work merges. Dated by
      the commit, so the request that merged it can fall in a later period.
      Merge commits excluded.
    unit: commits
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_commit_count
    dimensions:
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_commits
    source_key: git
    subject: commits
    label: Commits that have not reached the default branch
    short_label: Commits (other)
    description: Authored commits that have not reached the default branch
    explanation: >-
      Authored commits not reachable from the repository's default branch —
      work in flight, or abandoned. A commit leaves this metric when its branch
      lands, so a past period's figure falls as work merges. Dated by the
      commit, not by the merge. A source reporting no branch membership counts
      here. Merge commits excluded.
    unit: commits
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_commit_count
    dimensions:
      - project
      - repository
      - source
  - metric_key: git.code_lines
    source_key: git
    subject: code_changes
    label: Code lines added
    short_label: Code lines
    description: Lines added to code files
    explanation: >-
      Lines added to files classified as code — tests, configuration, and
      documentation excluded. Each change counts once: when the same content
      reaches a repository in more than one commit, the lines belong to the
      commit that introduced them first.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: code_lines_added
    dimensions:
      - branch_scope
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.default_branch_code_lines
    source_key: git
    subject: code_changes
    label: Code lines that reached the default branch
    short_label: Code lines (default)
    description: Lines added to code files that reached the default branch
    explanation: >-
      Code lines whose commit is reachable from the repository's default
      branch, or was carried onto it by a merged pull request. Lines follow
      their commit, so this rises as work merges. Dated by the commit, not by
      the merge. Tests, configuration and documentation excluded.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_code_lines_added
    dimensions:
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_code_lines
    source_key: git
    subject: code_changes
    label: Code lines that have not reached the default branch
    short_label: Code lines (other)
    description: Lines added to code files that have not reached the default branch
    explanation: >-
      Code lines whose commit has not reached the repository's default branch —
      work in flight, or abandoned. Lines follow their commit, so this falls as
      work merges. Dated by the commit, not by the merge. Tests, configuration
      and documentation excluded.
    unit: lines
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_code_lines_added
    dimensions:
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.lines_added
    source_key: git
    subject: code_changes
    label: Lines added
    short_label: Lines +
    description: All lines added, by file category
    explanation: >-
      Lines added across all files, split by file category: code, tests,
      configuration, documentation. Each change counts once: when the same
      content reaches a repository in more than one commit, the lines belong
      to the commit that introduced them first.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: lines_added
    dimensions:
      - branch_scope
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.default_branch_lines_added
    source_key: git
    subject: code_changes
    label: Lines added that reached the default branch
    short_label: Lines + (default)
    description: All lines added that reached the default branch
    explanation: >-
      Lines added whose commit is reachable from the repository's default
      branch, or was carried onto it by a merged pull request. Lines follow
      their commit, so this rises as work merges. Dated by the commit, not by
      the merge.
    unit: lines
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_lines_added
    dimensions:
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_lines_added
    source_key: git
    subject: code_changes
    label: Lines added that have not reached the default branch
    short_label: Lines + (other)
    description: All lines added that have not reached the default branch
    explanation: >-
      Lines added whose commit has not reached the repository's default branch —
      work in flight, or abandoned. Lines follow their commit, so this falls as
      work merges. Dated by the commit, not by the merge.
    unit: lines
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_lines_added
    dimensions:
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.test_change_share
    source_key: git
    subject: code_changes
    tags:
      - rate
    label: Test change share
    short_label: Test share
    description: Share of implementation and test additions made in test files
    explanation: Lines added to test files divided by lines added to code and test files. Documentation, configuration, and generated files do not affect the percentage. A result near zero can identify changes where tests are not evolving with implementation, but expected levels vary by repository and change type.
    format: percent
    direction: neutral
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: test_lines_added
      - input_role: denominator
        measure_key: test_and_code_lines_added
    dimensions:
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.lines_removed
    source_key: git
    subject: code_changes
    label: Lines removed
    short_label: Lines −
    description: All lines removed, by file category
    explanation: >-
      Lines removed across all reported file changes, with file-category,
      repository, and source breakdowns available. Each change counts once:
      when the same removal reaches a repository in more than one commit, the
      lines belong to the commit that made it first.
    unit: lines
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: lines_removed
    dimensions:
      - branch_scope
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.default_branch_lines_removed
    source_key: git
    subject: code_changes
    label: Lines removed that reached the default branch
    short_label: Lines − (default)
    description: All lines removed that reached the default branch
    explanation: >-
      Lines removed whose commit is reachable from the repository's default
      branch, or was carried onto it by a merged pull request. Lines follow
      their commit, so this rises as work merges. Dated by the commit, not by
      the merge.
    unit: lines
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_lines_removed
    dimensions:
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_lines_removed
    source_key: git
    subject: code_changes
    label: Lines removed that have not reached the default branch
    short_label: Lines − (other)
    description: All lines removed that have not reached the default branch
    explanation: >-
      Lines removed whose commit has not reached the repository's default
      branch — work in flight, or abandoned. Lines follow their commit, so this
      falls as work merges. Dated by the commit, not by the merge.
    unit: lines
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_lines_removed
    dimensions:
      - category
      - change_type
      - file_extension
      - project
      - repository
      - source
  - metric_key: git.prs_created
    source_key: git
    subject: pull_requests
    label: Pull requests created
    short_label: PRs opened
    description: Authored pull requests
    explanation: Pull requests opened, dated by creation.
    unit: PRs
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_created
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.default_branch_prs_created
    source_key: git
    subject: pull_requests
    label: Pull requests created into the default branch
    short_label: PRs opened (default)
    description: Authored pull requests targeting the default branch
    explanation: >-
      Pull requests opened against the repository's default branch, dated by
      creation. Unlike the commit and line splits this one is settled when the
      request is opened and does not move afterwards.
    unit: PRs
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_pr_created
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_prs_created
    source_key: git
    subject: pull_requests
    label: Pull requests created into another branch
    short_label: PRs opened (other)
    description: Authored pull requests targeting a branch other than the default
    explanation: >-
      Pull requests opened against something other than the repository's
      default branch — a release branch, a stacked request, an integration
      branch. Dated by creation. A request whose destination the source does
      not report counts here rather than inventing an answer.
    unit: PRs
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_pr_created
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.prs_merged
    source_key: git
    subject: pull_requests
    label: Pull requests merged
    short_label: PRs merged
    description: Authored pull requests merged
    explanation: >-
      Authored pull requests that merged, dated by the merge — so a request
      counts in the period it landed and can carry commits written in earlier
      ones. Under a branch-scope breakdown the split says where the request was
      aimed, not where its commits sit.
    unit: PRs
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_merged
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.default_branch_prs_merged
    source_key: git
    subject: pull_requests
    label: Pull requests merged into the default branch
    short_label: PRs merged (default)
    description: Authored pull requests merged into the default branch
    explanation: >-
      Pull requests that merged into the repository's default branch, dated by
      the merge. This is the surface that also promotes a branch's commits and
      lines into their default-branch metrics. A request counts in the period
      it merged, so it can carry commits written in earlier ones.
    unit: PRs
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: default_pr_merged
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.non_default_branch_prs_merged
    source_key: git
    subject: pull_requests
    label: Pull requests merged into another branch
    short_label: PRs merged (other)
    description: Authored pull requests merged into a branch other than the default
    explanation: >-
      Pull requests that merged into something other than the repository's
      default branch, dated by the merge. Their commits do not count as having
      reached the default branch on this evidence alone. A request counts in the
      period it merged, so it can carry commits written in earlier ones.
    unit: PRs
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: non_default_pr_merged
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.merge_rate
    source_key: git
    subject: pull_requests
    tags:
      - rate
    label: PR merge rate
    short_label: Merge %
    description: Share of created pull requests that merged
    explanation: Of the pull requests created in the period, the share that have merged. Requests opened near the end of the period may not have merged yet, which lowers the rate at period edges.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_created_merged
      - input_role: denominator
        measure_key: pr_created
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.pr_abandonment_rate
    source_key: git
    subject: pull_requests
    tags:
      - rate
    label: PR abandonment rate
    short_label: Abandoned
    description: Share of created pull requests closed without merging
    explanation: Of pull requests created in the period, the share now closed without merging. Open requests are not abandoned. Requests created near the period end can still change outcome later.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_abandoned
      - input_role: denominator
        measure_key: pr_created
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.review_coverage
    source_key: git
    subject: pull_requests
    tags:
      - rate
      - review
    label: Review coverage
    short_label: Reviewed
    description: Share of created pull requests with at least one acting reviewer
    explanation: >-
      Of pull requests created in the period, the share with at least one
      submitted review or approval. Assigned reviewers who never act do not
      count. Some sources provide approvals without review timestamps.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_reviewed
      - input_role: denominator
        measure_key: pr_created
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.reviewers_per_pr
    source_key: git
    subject: pull_requests
    tags:
      - rate
      - review
    label: Reviewers per PR
    description: Average number of acting reviewers per created pull request
    explanation: Distinct reviewers who submitted a review or approval, divided by pull requests created in the period. Pull requests without review contribute zero, so read this with review coverage to separate breadth from coverage.
    format: decimal
    direction: neutral
    entity_type: person
    computation: !ratio
      scale: 1.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_reviewer_count
      - input_role: denominator
        measure_key: pr_created
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.multi_reviewer_rate
    source_key: git
    subject: pull_requests
    tags:
      - rate
      - review
    label: Multi-reviewer rate
    short_label: Multi-review
    description: Share of reviewed pull requests with more than one acting reviewer
    explanation: Of pull requests with at least one submitted review or approval, the share with two or more distinct acting reviewers. This measures review breadth without conflating it with unreviewed pull requests.
    format: percent
    direction: neutral
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_multi_reviewed
      - input_role: denominator
        measure_key: pr_reviewed
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.merges_without_approval_rate
    source_key: git
    subject: pull_requests
    tags:
      - rate
      - review
    label: Merges without approval
    short_label: No approval
    description: Share of merged pull requests with no reported approval
    explanation: Of pull requests merged in the period, the share with no approval reported by the connected source. This can reveal missing approval gates, but source configuration and incomplete review history can also affect it.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: pr_merged_without_approval
      - input_role: denominator
        measure_key: pr_merged
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.active_days
    source_key: git
    subject: commits
    label: Active commit days
    short_label: Active days
    description: Distinct days with at least one authored commit
    explanation: Distinct calendar days with authored, non-merge commits. Repository breakdowns count active days independently; the total still counts each calendar day once across repositories.
    unit: days
    format: integer
    direction: neutral
    entity_type: person
    computation: distinct_count
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: commit_day
    dimensions:
      - project
      - repository
      - source
  - metric_key: git.commits_per_active_day
    source_key: git
    subject: commits
    tags:
      - rate
    label: Commits per active day
    short_label: Commits/day
    description: Commit cadence on days with commits
    explanation: Commits divided by the number of days with at least one commit.
    format: decimal
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 1.0
      denominator_aggregation: distinct_count
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: commit_count
      - input_role: denominator
        measure_key: commit_day
    dimensions:
      - project
      - repository
      - source
  - metric_key: git.commit_size
    source_key: git
    subject: commits
    tags:
      - distribution
    label: Commit size
    description: Typical diff size per commit
    explanation: >-
      Median diff size of authored commits (lines added plus removed),
      counting only content a commit is the first to introduce — a commit
      that repeats content already in the repository has a size of zero.
      Smaller commits are easier to review.
    unit: lines
    format: integer
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: commit_change_size
    dimensions:
      - branch_scope
      - project
      - repository
      - source
  - metric_key: git.pr_size
    source_key: git
    subject: pull_requests
    tags:
      - distribution
    label: PR size
    description: Typical diff size per pull request
    explanation: Median diff size of authored pull requests (lines added plus removed). Smaller requests are easier to review. Sources that do not report line counts contribute no values.
    unit: lines
    format: integer
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_change_size
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.pr_commits
    source_key: git
    subject: pull_requests
    tags:
      - distribution
    label: Commits per PR
    short_label: Commits/PR
    description: Typical number of commits per merged pull request
    explanation: Median count of commits linked to authored pull requests, over requests merged in the period. A merged request whose commits the source does not link contributes no value.
    unit: commits
    format: decimal
    direction: neutral
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_commit_count
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.pr_cycle_time_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
    label: PR cycle time
    short_label: PR cycle
    description: Typical hours from open to merge
    explanation: Median hours from opening a pull request to merging it, over requests merged in the period.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_cycle_hours
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.pr_cycle_time_p75_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
    label: PR cycle time (p75)
    short_label: Cycle p75
    description: Slow-end hours from open to merge
    explanation: 75th percentile of hours from opening a pull request to merging it, over requests merged in the period.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: !percentile
      q: 0.75
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_cycle_hours
    dimensions:
      - branch_scope
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.first_review_time_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
      - review
    label: Time to first review
    short_label: First review
    description: Typical hours from pull request creation to first review
    explanation: Median hours from opening a pull request to its first submitted review, over first reviews recorded in the period. Pull requests without a review and sources without review timestamps contribute no duration.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_first_review_hours
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.first_review_time_p75_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
      - review
    label: Time to first review (p75)
    short_label: First review p75
    description: Slow-end hours from pull request creation to first review
    explanation: 75th percentile of hours from opening a pull request to its first submitted review, over first reviews recorded in the period. Pull requests without a review and sources without review timestamps contribute no duration.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: !percentile
      q: 0.75
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_first_review_hours
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.review_wait_share
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
      - review
    label: Review wait share
    short_label: Review wait
    description: Typical share of pull request cycle time spent waiting for first review
    explanation: Median percentage of open-to-merge time elapsed before the first submitted review, over merged pull requests in the period. Sources without review timestamps contribute no value. High values suggest first review is the main cycle-time constraint.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_review_wait_share
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.review_to_merge_time_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
      - review
    label: Review-to-merge time
    short_label: Review to merge
    description: Typical hours from first review to merge
    explanation: Median hours from first submitted review to merge, over merged pull requests in the period. Sources without review timestamps contribute no value. Read with time to first review to locate delay before or after review starts.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_review_to_merge_hours
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.approval_to_merge_time_h
    source_key: git
    subject: pull_requests
    tags:
      - duration
      - distribution
      - review
    label: Approval-to-merge time
    short_label: Approval to merge
    description: Typical hours from latest timestamped approval to merge
    explanation: Median hours from the latest reported approval with a timestamp to merge, over merged pull requests in the period. Sources that expose approvals without per-approval timestamps contribute no value. High values can identify delay after review gates clear.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_approval_to_merge_hours
    dimensions:
      - destination_branch
      - project
      - repository
      - source
  - metric_key: git.reviews_performed
    source_key: git
    subject: pull_requests
    tags:
      - review
    label: Reviews performed
    short_label: Reviews
    description: Review verdicts submitted on pull requests
    explanation: Review verdicts the person submitted on any pull request, dated by submission. This is the reviewer's side of review coverage — it counts the reviews a person performed, not the reviews their own requests received.
    unit: reviews
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: review_submitted
    dimensions:
      - project
      - repository
      - source
  - metric_key: git.pr_comments
    source_key: git
    subject: pull_requests
    tags:
      - review
    label: PR comments
    short_label: PR comments
    description: Comments written on pull requests
    explanation: Comments the person wrote on pull requests — conversation and inline review comments alike — dated by posting. The comment target dimension splits comments on the person's own requests from comments on other people's; a comment whose request author cannot be determined counts under others, so the two halves always add up to the total.
    unit: comments
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pr_comment
    dimensions:
      - comment_target
      - project
      - repository
      - source
  - metric_key: collab.messages_sent
    source_key: collab
    subject: messaging
    label: Messages Sent
    short_label: Msgs
    description: Chat messages sent
    explanation: 'Chat messages a person sent across messaging tools. Counts are not directly comparable between tools: Slack includes thread replies, and Microsoft 365 combines private-chat and channel messages.'
    unit: messages
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: total_chat_messages
    dimensions:
      - tool
  - metric_key: collab.channel_posts
    source_key: collab
    subject: messaging
    label: Channel Posts
    short_label: Channel posts
    description: Messages posted to shared channels, including replies
    explanation: Channel posts plus thread replies across messaging tools. Tools that report posts and replies separately are folded so counts stay comparable.
    unit: messages
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: channel_posts
    dimensions:
      - tool
  - metric_key: collab.dm_ratio
    source_key: collab
    subject: messaging
    tags:
      - rate
    label: DM Ratio
    short_label: DM %
    description: Share of messages sent in direct or group chats
    explanation: Direct and group-chat messages divided by all chat messages. A lower ratio means more communication happens in open channels. Tools that do not distinguish message types report no value.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: direct_and_group_messages
      - input_role: denominator
        measure_key: total_chat_messages
    dimensions:
      - tool
  - metric_key: collab.msgs_per_active_day
    source_key: collab
    subject: messaging
    tags:
      - rate
    label: Messages per Active Day
    short_label: Msgs/day
    description: Chat messages divided by chat-active days
    explanation: Chat messages sent divided by days with chat messages. Each tool's active days count separately.
    unit: messages/day
    format: decimal
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 1.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: total_chat_messages
      - input_role: denominator
        measure_key: chat_active_day
    dimensions:
      - tool
  - metric_key: collab.active_days
    source_key: collab
    subject: activity
    label: Active Days
    description: Days with collaboration activity
    explanation: Distinct days on which a person took a deliberate collaboration action — sending a message, sending email, engaging or sharing a file, or attending a meeting. Passive activity such as receiving or reading email is excluded.
    unit: days
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: distinct_count
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: active_day
    dimensions:
      - tool
  - metric_key: collab.emails_sent
    source_key: collab
    subject: email
    label: Emails Sent
    short_label: Emails sent
    description: Emails sent
    explanation: Emails a person sent.
    unit: emails
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: emails_sent
    dimensions:
      - tool
  - metric_key: collab.emails_received
    source_key: collab
    subject: email
    label: Emails Received
    short_label: Emails rcvd
    description: Emails received
    explanation: Emails a person received.
    unit: emails
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: emails_received
    dimensions:
      - tool
  - metric_key: collab.emails_read
    source_key: collab
    subject: email
    label: Emails Read
    short_label: Emails read
    description: Emails read
    explanation: Emails a person read.
    unit: emails
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: emails_read
    dimensions:
      - tool
  - metric_key: collab.files_engaged
    source_key: collab
    subject: documents
    label: Files Engaged
    description: Files viewed or edited
    explanation: Files a person viewed or edited.
    unit: files
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: files_engaged
    dimensions:
      - tool
  - metric_key: collab.files_shared_internal
    source_key: collab
    subject: documents
    label: Files Shared (Internal)
    short_label: Files (int)
    description: Files shared inside the organization
    explanation: Files a person shared with people inside the organization.
    unit: files
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: files_shared_internal
    dimensions:
      - tool
  - metric_key: collab.files_shared_external
    source_key: collab
    subject: documents
    label: Files Shared (External)
    short_label: Files (ext)
    description: Files shared outside the organization
    explanation: Files a person shared with people outside the organization.
    unit: files
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: files_shared_external
    dimensions:
      - tool
  - metric_key: collab.files_shared
    source_key: collab
    subject: documents
    label: Files Shared
    short_label: Files shared
    description: Files shared with any recipient
    explanation: Files a person shared with recipients inside or outside the organization.
    unit: files
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: files_shared
    dimensions:
      - scope
  - metric_key: collab.meeting_hours
    source_key: collab
    subject: meetings
    tags:
      - duration
    label: Meeting Hours
    short_label: Mtg hrs
    description: Hours spent in meetings
    explanation: Hours spent in meetings, taking the longest active modality (audio, video, or screen share) per meeting. Zoom reports modality durations as full-session estimates, so its figures may run higher than Microsoft Teams.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: meeting_hours
    dimensions:
      - tool
  - metric_key: collab.meetings_count
    source_key: collab
    subject: meetings
    label: Meetings Attended
    short_label: Mtgs
    description: Distinct meetings attended
    explanation: Distinct meetings a person attended across meeting tools.
    unit: meetings
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: meetings_attended
    dimensions:
      - tool
  - metric_key: collab.meeting_free_days
    source_key: collab
    subject: meetings
    label: Meeting-Free Days
    short_label: Mtg-free days
    description: Active days with no meeting time
    explanation: Days on which a person was actively collaborating but spent no time in meetings — a proxy for uninterrupted working days.
    unit: days
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: meeting_free_day
    dimensions: []
  - metric_key: collab.focus_time_pct
    source_key: collab
    subject: focus
    tags:
      - rate
    label: Focus Time
    short_label: Focus %
    description: Share of the workday outside meetings
    explanation: 'Share of the workday not spent in meetings: meeting-free hours divided by scheduled working hours. Scheduled hours default to a nominal eight-hour day where an HR source does not provide them.'
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: focus_hours
      - input_role: denominator
        measure_key: working_hours
    dimensions: []
  - metric_key: collab.breadth
    source_key: collab
    subject: activity
    label: Collaboration Breadth
    short_label: Breadth
    description: Distinct collaboration modalities used
    explanation: Distinct collaboration modalities — chat, meetings, email, documents — a person was deliberately active in during the period.
    unit: modalities
    format: integer
    direction: neutral
    entity_type: person
    computation: distinct_count
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: active_modality
    dimensions: []
  - metric_key: collab.meetings_organized
    source_key: collab
    subject: meetings
    label: Meetings Organized
    short_label: Mtgs hosted
    description: Meetings organized
    explanation: Meetings a person organized. Reported only by tools that expose organizer counts.
    unit: meetings
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: meetings_organized
    dimensions:
      - tool
  - metric_key: collab.adhoc_meetings
    source_key: collab
    subject: meetings
    label: Ad-hoc Meetings
    short_label: Ad-hoc mtgs
    description: Unscheduled meetings attended
    explanation: Unscheduled meetings a person attended. Reported only by tools that distinguish ad-hoc from scheduled meetings.
    unit: meetings
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: adhoc_meetings_attended
    dimensions:
      - tool
  - metric_key: collab.scheduled_meetings
    source_key: collab
    subject: meetings
    label: Scheduled Meetings
    short_label: Sched. mtgs
    description: Scheduled meetings attended
    explanation: Scheduled meetings a person attended. Reported only by tools that distinguish ad-hoc from scheduled meetings.
    unit: meetings
    format: integer
    direction: neutral
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: scheduled_meetings_attended
    dimensions:
      - tool
  - metric_key: tasks.closed
    source_key: task
    subject: throughput
    label: Issues closed
    short_label: Issues
    description: Every issue closed in the tracker
    explanation: >-
      All issues a person moved into a closed status during the period. Bugs are part of this number and are listed separately by type.
    unit: issues
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: tasks_closed
    dimensions:
      - type
      - source
  - metric_key: tasks.bugs_fixed
    source_key: task
    subject: quality
    label: Bugs closed
    short_label: Bugs
    description: Bug-type issues closed
    explanation: >-
      Issues of a bug type a person closed during the period. Part of issues closed, not a separate total.
    unit: issues
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: bugs_fixed
    dimensions: []
  - metric_key: tasks.closed_non_bug
    source_key: task
    subject: throughput
    label: Non-bug issues closed
    short_label: Non-bug
    description: Closed issues of a known non-bug type
    explanation: >-
      Issues of a known non-bug type a person closed during the period. Issues whose type cannot be determined are excluded rather than counted here.
    unit: issues
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: closed_non_bug
    dimensions: []
  - metric_key: tasks.dev_time
    source_key: task
    subject: cycle_time
    tags:
      - duration
      - distribution
    label: Development time
    short_label: Dev time
    description: Time an issue spends in active development
    explanation: Median time closed issues spent in in-progress statuses, from first pickup to close.
    unit: h
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: dev_time_hours
    dimensions:
      - type
      - source
  - metric_key: tasks.resolution_time
    source_key: task
    subject: cycle_time
    tags:
      - duration
      - distribution
    label: Time to resolution
    short_label: Resolution
    description: Issue lifetime from creation to close
    explanation: Median time from issue creation to close.
    unit: d
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: resolution_days
    dimensions:
      - type
      - source
  - metric_key: tasks.pickup_time
    source_key: task
    subject: cycle_time
    tags:
      - duration
      - distribution
    label: Pickup time
    short_label: Pickup
    description: Wait before work starts on an issue
    explanation: Median time from issue creation to first entering an in-progress status.
    unit: d
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: median
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pickup_days
    dimensions:
      - type
      - source
  - metric_key: tasks.flow_efficiency
    source_key: task
    subject: cycle_time
    tags:
      - rate
    label: Flow efficiency
    short_label: Flow %
    description: Active development share of issue lifetime
    explanation: Time in active development as a share of total issue lifetime, across closed issues.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    transform:
      clamp_max: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: flow_dev_seconds
      - input_role: denominator
        measure_key: flow_lead_seconds
    dimensions: []
  - metric_key: tasks.reopen_rate
    source_key: task
    subject: quality
    tags:
      - rate
    label: Reopen rate
    short_label: Reopen %
    description: Closed issues reopened shortly after
    explanation: Share of issue closes followed by a reopen within 14 days.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: reopened_within_14d
      - input_role: denominator
        measure_key: close_events
    dimensions: []
  - metric_key: tasks.due_date_compliance
    source_key: task
    subject: predictability
    tags:
      - rate
    label: Due date compliance
    short_label: Due date %
    description: On-time share of issues with a due date
    explanation: Share of issues that had a due date and were closed on or before it.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: due_date_on_time
      - input_role: denominator
        measure_key: due_date_with_due
    dimensions: []
  - metric_key: tasks.on_time_delivery
    source_key: task
    subject: predictability
    tags:
      - rate
    label: On-time delivery
    short_label: On-time %
    description: On-time share of all closed issues
    explanation: Share of all closed issues that were closed on or before their due date.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: due_date_on_time
      - input_role: denominator
        measure_key: tasks_closed
    dimensions: []
  - metric_key: tasks.avg_slip
    source_key: task
    subject: predictability
    tags:
      - rate
      - duration
    label: Average slip
    short_label: Avg slip
    description: How late overdue issues close
    explanation: Average days past the due date for issues closed late.
    unit: d
    format: decimal
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 1.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: slip_days_total
      - input_role: denominator
        measure_key: late_count
    dimensions: []
  - metric_key: tasks.estimation_accuracy
    source_key: task
    subject: predictability
    tags:
      - rate
    label: Estimation accuracy
    short_label: Estimate %
    description: How close estimates land to time spent
    explanation: 100 minus the average deviation between original estimates and time spent, over days whose estimated work stayed within twice the estimate. 100 means estimates matched reality; over- and under-estimation count equally.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 1.0
    transform:
      multiplier: -1.0
      offset: 100.0
      clamp_min: 0.0
      clamp_max: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: estimation_error_pct
      - input_role: denominator
        measure_key: estimation_samples
    dimensions: []
  - metric_key: tasks.worklog_accuracy
    source_key: task
    subject: predictability
    tags:
      - rate
    label: Worklog accuracy
    short_label: Worklog %
    description: Logged time versus tracked development
    explanation: Logged work time as a share of time issues spent in in-progress statuses.
    format: percent
    direction: higher_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    transform:
      clamp_max: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: worklog_seconds
      - input_role: denominator
        measure_key: in_progress_seconds
    dimensions: []
  - metric_key: tasks.bugs_ratio
    source_key: task
    subject: quality
    tags:
      - rate
    label: Bugs share of closed issues
    short_label: Bug share
    description: Bugs as a share of all closed issues
    explanation: >-
      Bug-type issues as a share of all closed issues, issues of an undetermined type included in the denominator. A share, so it cannot exceed 100%.
    format: percent
    direction: lower_is_better
    entity_type: person
    computation: !ratio
      scale: 100.0
    peer_cohort_key: org_unit
    inputs:
      - input_role: numerator
        measure_key: bugs_fixed
      - input_role: denominator
        measure_key: tasks_closed
    dimensions: []
  - metric_key: tasks.stale_in_progress
    source_key: task
    subject: throughput
    label: Stale in progress
    short_label: Stale WIP
    description: Open issues idle for over two weeks
    explanation: Open issues with no status change in more than 14 days.
    unit: issues
    format: integer
    direction: lower_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: stale_in_progress
    dimensions: []
  - metric_key: wiki.pages_created
    source_key: wiki
    subject: authoring
    label: Pages created
    short_label: New pages
    description: Wiki pages authored
    explanation: Wiki pages the person created during the period, counted on the page's creation date.
    unit: pages
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pages_created
    dimensions: []
  - metric_key: wiki.edits
    source_key: wiki
    subject: authoring
    label: Page edits
    short_label: Edits
    description: Wiki edit sessions
    explanation: Logical wiki edits the person made during the period. Consecutive saves of the same page within a short window count as one edit, so autosaves do not inflate the number.
    unit: edits
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: edits
    dimensions: []
  - metric_key: wiki.pages_edited
    source_key: wiki
    subject: authoring
    label: Pages edited
    short_label: Pages edited
    description: Distinct wiki pages edited
    explanation: Distinct wiki pages the person edited during the period, counted per day the page was touched.
    unit: pages
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: pages_edited
    dimensions: []
  - metric_key: wiki.comments
    source_key: wiki
    subject: engagement
    label: Comments received
    short_label: Comments
    description: Comments on the person's wiki pages
    explanation: Comments and replies other people left on wiki pages the person authored — a signal of how much their documentation is read and discussed.
    unit: comments
    format: integer
    direction: higher_is_better
    entity_type: person
    computation: sum
    peer_cohort_key: org_unit
    inputs:
      - input_role: value
        measure_key: comments
    dimensions: []
