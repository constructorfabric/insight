//! The question endpoints: one route per kind of question the semantic
//! definitions can answer. Values are the first; comparisons, distributions,
//! and rows are the same shape of ask against the same definitions.

pub(crate) mod values;
