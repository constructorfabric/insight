apiVersion: v2
name: insight-git-cli-proxy
description: Insight Git CLI Proxy — clone-based git data extraction for the nocode git connectors
version: 0.1.0
# appVersion is managed by the release pipeline (build-images.yml): it is
# bumped to the freshly built image tag on merges that change this service.
appVersion: "2026.08.25.11.39-d0d4698"
type: application
