apiVersion: v2
name: insight-v3-core
description: Insight v3 Core — token-authenticated raw-data ingestion
version: 0.1.0
# appVersion is managed by the release pipeline (build-images.yml): it is
# bumped to the freshly built image tag on merges that change this service.
appVersion: "2026.09.18.18.30-39f3232"
type: application
