"""Insight sample-data seeder.

Populates a stand — the compose stack or a chart-deployed Kubernetes stand —
with a demo organisation and its activity. `__main__` is the entry point
(`python -m insight_seed <step>`); `README.md` one level up documents the flags,
the environment contract and what a seeded stand contains.
"""
