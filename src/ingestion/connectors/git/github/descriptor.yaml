name: github
# Strict semver per ADR-0015. 5.0.0: dbt staging carries the diff-stats
#   author account id into the class-contract column author_account_id, the
#   key pull-request person attribution resolves through. MAJOR — the id has
#   been in Bronze all along, but staging is incremental on
#   _airbyte_extracted_at and never re-reads Bronze, so the column heals to ''
#   on existing rows and history would never attribute. Only the one-shot
#   scoped `dbt --full-refresh` a major bump dispatches re-materializes it.
#   Safe: every git staging model is a pure projection of Bronze.
# 6.0.0: dbt staging carries the per-commit patch id into the class-contract
#   column patch_id, the commit-content identity that folds a rebase copy or a
#   cherry-pick onto the commit that first authored the change. MAJOR for the
#   same reason as 5.0.0 — the patch id has been in Bronze all along, but
#   staging never re-reads Bronze, so the column heals to NULL on existing
#   rows, and a NULL identity never folds. Only the one-shot scoped
#   `dbt --full-refresh` a major bump dispatches re-materializes the history.
# 6.1.0: Projects V2 boards — two new streams (`project_fields`,
#   `project_items`) and three new columns on `issue_timeline_events`
#   (`project_id`, `project_number`, `was_automated`) plus two new event types
#   on it. MINOR: additive everywhere, and the one model that consumes the new
#   columns is materialized as a table, so it re-reads Bronze on every run and
#   needs no full refresh. The new columns are only POPULATED for events
#   collected after the change — recovering them for history means clearing the
#   `issue_timeline_events` cursor and re-walking, which is a rollout step, not
#   a version bump.
# 7.0.0: work-item links, and a correction to how the two Projects V2 streams
#   are keyed. MAJOR for the keys: `project_fields` and `project_items` no
#   longer carry a collection day in `unique_key`, so bronze holds the present
#   and the ReplacingMergeTree collapses onto it, with history moved to the
#   shared SCD2 snapshot macros (ADR-0001, ADR-0004 forbid a version in the
#   key). Rows written under the old day-keyed scheme cannot collapse onto the
#   new ones and must be dropped: TRUNCATE both bronze relations before the
#   first sync on this version. `project_fields` also loses `snapshot_date`;
#   the column survives on existing installations as NULL because nothing
#   drops a bronze column.
#   Additive alongside: a new `issue_links` stream, twelve link event types on
#   `issue_timeline_events`, and four columns for the link target.
# 8.0.0: the class-contract column `date` now carries the AUTHOR date rather
#   than the committer date, and `committer_date` joins it as its own column.
#   A rebase, an amend and a cherry-pick rewrite the committer date and keep
#   the author date, so a branch rebased before merging used to collapse every
#   day of work on it onto the day of the last rewrite. MAJOR for the same
#   reason as the patch id: staging is incremental on _airbyte_extracted_at and
#   never re-reads Bronze, so already-materialized commits keep the committer
#   date forever and the install would run in two regimes. Only the one-shot
#   scoped `dbt --full-refresh` a major bump dispatches re-materializes them.
#   Safe: both dates have been in Bronze all along, and the author date falls
#   back to the committer date where a source left it empty. #3153
version: "8.0.0"
schedule: "0 */4 * * *"
workflow: sync

# Silver routing via dbt tags on the staging models.
# silver_targets prohibited per Connector Spec §4.10 — Silver is determined by
# the dbt tags, and the pipeline's silver step hardcodes `tag:<slug>+`.
dbt_select: "tag:github+"

# The proxy's address and bearer token belong to the platform, not to the
# operator: the chart owns both and publishes them into the reconcile CronJob,
# which injects them into this connector's source config. A Secret carrying them
# would be a second copy of a port and a token that the chart can change.
platform_config:
  git_proxy: true

secret:
  # The pre-flight Secret validator reads this list and nothing else, so it
  # carries every field the operator must supply — the proxy address and token
  # are not among them, because platform_config.git_proxy injects those.
  required_fields:
    - github_token
    - github_organizations
    - github_start_date

connection:
  namespace: bronze_github
