# Copy this file to `.env.local` and fill in values as needed.
# `.env.local` is gitignored — do not commit tenant secrets.

# Vite dev server proxy for /api and /auth requests. Defaults to the compose
# gateway (http://localhost:8080) under `pnpm dev`; set only to point at a
# different backend (e.g. a kubectl port-forward). Builds ignore this.
# VITE_API_PROXY_TARGET=http://localhost:8080

# Session token for running `pnpm dev` against a DEPLOYED backend. The stand
# registers its OIDC redirect_uri on its own origin, so the login leg can never
# finish on localhost; instead paste a session you already hold and the dev
# proxy attaches it to every /api and /auth request, absorbing the rotation
# that /auth/refresh performs. Sign in to the stand in a browser and copy the
# `__Host-sid` value from DevTools > Application > Cookies.
# THIS IS A LIVE SESSION ON THAT STAND — `.env.local` only, never commit it.
# Unset (the default) leaves proxying and the login flow exactly as they are.
# VITE_API_PROXY_SESSION=

# Skip TLS verification for the proxy upstream. Only for a stand serving a
# self-signed certificate.
# VITE_API_PROXY_INSECURE=true

# API base paths baked into the clients. Defaults match the gateway mounts;
# override only for non-standard deployments.
# VITE_API_BASE=/api/analytics/v1
# VITE_IDENTITY_BASE=/api/identity/v1

# Mock API plugins — OFF by default. Set to "true" ONLY when demoing without a
# backend or running UI fixtures. The app renders a yellow warning strip
# whenever mocks are active so synthetic values are never mistaken for real
# data. Prod builds ignore this flag entirely (dead-code eliminated).
VITE_ENABLE_MOCKS=false

# Suppress the mock-data warning banner while VITE_ENABLE_MOCKS is still on.
# Set during screenshot / demo capture when the banner would intrude on the
# image. Mocks themselves keep running. No effect when mocks are off.
# VITE_HIDE_MOCK_BANNER=true

# Sentry DSN, local only — a deployed stand gets it from the chart's
# `sentry.dsn`. Unset means no reporting, the normal state for local work.
# VITE_SENTRY_DSN=https://<key>@sentry.example.com/<project-id>
